version: "3"
services:
  dev:
    volumes:
      - ./src:/app/src
    build:
      context: .
    ports:
      - 80:3000
    command: npm start
    #Needed to be able to run the WebPack CRA console if not there it will crash
